# بهینه‌سازی مصرف انرژی در شبکه‌های LoRaWAN با استفاده از یادگیری ماشین

این مخزن شامل پیاده‌سازی کامل یک **Pipeline انتهابه‌انتها برای بهینه‌سازی مصرف انرژی در شبکه‌های LoRaWAN** با استفاده از **یادگیری ماشین** است.
ایده‌ی اصلی پروژه، **پیش‌بینی کیفیت لینک (SNR)** و سپس **کنترل تطبیقی توان ارسال (Transmission Power Control)** به‌منظور کاهش مصرف انرژی، در عین حفظ حاشیه‌ی اطمینان لینک می‌باشد.

---

## 🎯 هدف پروژه

در شبکه‌های LoRaWAN، الگوریتم‌های استانداردی مانند ADR معمولاً:

* واکنش کندی به تغییرات محیط دارند
* محافظه‌کارانه عمل می‌کنند
* باعث افزایش غیرضروری مصرف انرژی می‌شوند

هدف این پروژه:

* پیش‌بینی SNR با مدل‌های یادگیری ماشین
* تنظیم هوشمند **Spreading Factor (SF)** و **Transmission Power (TP)**
* کاهش مصرف انرژی با حفظ قابلیت ارتباط پایدار

---

## 📚 فایل پژوهشی پروژه

گزارش کامل پژوهشی این پروژه (بیش از ۳۰ صفحه) در فایل زیر قرار دارد و مبنای علمی پیاده‌سازی حاضر است:

📄 **Energy_Optimization_IOT_Using ML.pdf**

در این گزارش:

* مسئله مصرف انرژی و مقیاس‌پذیری در LoRaWAN بررسی شده
* محدودیت‌های ADR استاندارد تحلیل شده
* رویکردهای مبتنی بر ML و RL مرور شده‌اند
* معیارهای انرژی و تصمیم‌گیری تطبیقی توضیح داده شده است

---

## 🧠 شرح کلی کاری که انجام شده است

این پروژه یک چارچوب ماژولار و قابل اجرا ارائه می‌دهد که مراحل زیر را پوشش می‌دهد:

### 1️⃣ پیش‌پردازش داده

* بارگذاری داده‌های LoRaWAN
* بررسی سلامت داده‌ها (ستون‌ها، NaN، نوع داده)
* آماده‌سازی داده برای مدل‌های ML

### 2️⃣ آموزش مدل‌های یادگیری ماشین

چند مدل پایه برای پیش‌بینی SNR آموزش داده شده‌اند:

* Ridge Regression
* Random Forest
* Support Vector Regression (SVR)

مدل‌ها با معیارهای **RMSE** و **R²** ارزیابی شده و مدل منتخب ذخیره می‌شود.

### 3️⃣ پیش‌بینی SNR

* استفاده از مدل آموزش‌دیده برای تخمین SNR
* جایگزینی تصمیم‌گیری مبتنی بر تاریخچه ADR با پیش‌بینی داده‌محور

### 4️⃣ کنترل توان ارسال (TPC)

* اعمال منطق TPC بر اساس SNR پیش‌بینی‌شده
* انتخاب SF و TP مناسب با در نظر گرفتن Link Margin

### 5️⃣ محاسبه مصرف انرژی

* تعریف یک معیار نسبی برای مصرف انرژی
* نرمال‌سازی انرژی برای مقایسه تصمیم‌ها

### 6️⃣ تولید خروجی‌ها

* ذخیره تصمیم‌های TPC در فایل‌های CSV
* تولید نمودارهای تحلیلی (SNR، SF، TP، انرژی)

---

## 🏗️ معماری و ساختار پروژه

```
.
├── data/
│   └── raw/                   داده‌های ورودی
├── models_trained/            مدل‌های آموزش‌دیده (joblib)
├── notebooks/
│   └── 01_demo_end_to_end.ipynb
├── outputs/
│   ├── predictions/           خروجی‌های عددی (CSV)
│   └── figures/               نمودارها
├── src/
│   ├── config.py              تنظیمات مرکزی پروژه
│   ├── io_utils.py            توابع کمکی ورودی/خروجی
│   ├── train_baselines.py     آموزش مدل‌های ML
│   ├── run_pipeline.py        اجرای کامل Pipeline
│   ├── tpc.py                 منطق کنترل توان ارسال
│   ├── energy.py              محاسبه معیار انرژی
│   └── summarize_results.py   خلاصه‌سازی نتایج
├── Energy_Optimization_IOT_Using ML.pdf
├── requirements.txt
└── README.md
```

---

## ▶️ نحوه اجرای پروژه

### 1️⃣ نصب پیش‌نیازها

```bash
pip install -r requirements.txt
```

### 2️⃣ بررسی اولیه داده‌ها (اختیاری)

```bash
python -m src.sanity_check
```

### 3️⃣ آموزش مدل‌های یادگیری ماشین

```bash
python -m src.train_baselines
```

### 4️⃣ اجرای Pipeline کامل

```bash
python -m src.run_pipeline
```

در این مرحله:

* SNR پیش‌بینی می‌شود
* تصمیم‌های SF و TP اعمال می‌شود
* مصرف انرژی محاسبه می‌شود
* نمودارها تولید می‌شوند

### 5️⃣ خلاصه‌سازی نتایج

```bash
python -m src.summarize_results
```

---

## 📊 خروجی‌ها

تمام خروجی‌ها در پوشه `outputs/` ذخیره می‌شوند، شامل:

* نمودار مقایسه SNR واقعی و پیش‌بینی‌شده
* توزیع SF و TP
* معیارهای مصرف انرژی
* فایل‌های CSV نتایج

---

## 🎓 جمع‌بندی

در این پروژه نشان داده شده است که:

* پیش‌بینی SNR با یادگیری ماشین
* می‌تواند تصمیم‌های تطبیقی بهتری نسبت به ADR استاندارد ایجاد کند
* و منجر به کاهش مصرف انرژی بدون افت شدید در قابلیت تحویل بسته‌ها شود

این مخزن به‌گونه‌ای طراحی شده که:

* قابل بررسی مستقیم توسط استاد باشد
* پروژه به‌صورت کامل قابل اجرا و بازتولید باشد
* ارتباط بین گزارش پژوهشی و پیاده‌سازی عملی شفاف باشد

---

## 👤 مشخصات دانشجو

نام دانشجو: …
درس: شبکه‌های بی‌سیم
مقطع: کارشناسی ارشد
